<?xml version="1.0" encoding="utf-8"?>
<!-- STATUS -> три кнопки статуса ранений (roadmap, "Единый компонент бокового меню
     3 уровня") — SidebarMenuAdapter вместо 3 вручную скопированных ConstraintLayout-блоков,
     тот же приём, что у SPECIAL/Skills/etc. Только 3 пункта, не листается — wrap_content,
     без ScrollView. -->
<androidx.recyclerview.widget.RecyclerView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recycler_tab_status_buttons"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" />
